import express from 'express'

const router = express.Router()

router.get('/inventory', (req, res) => {
  // TODO: make call to scanngo api to get inventory
  // for clientid
})

export default router